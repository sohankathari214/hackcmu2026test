# Server-only OpenAI credentials. Never expose this key in NEXT_PUBLIC_* variables.
OPENAI_API_KEY=
# Cheap default for structured extraction. Override only when necessary.
OPENAI_MODEL=gpt-5-nano
# Optional server-side API destination for a separately hosted backend.
BACKEND_API_URL=http://127.0.0.1:8000
